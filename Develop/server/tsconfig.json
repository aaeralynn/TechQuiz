{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "composite": true,

    /* Modules */
    "rootDir": "src",
    "outDir": "dist",

    /* Override frontend stuff */
    "types": ["node"],

    /* Optional: avoid compiling unused React/DOM stuff */
    "lib": ["ES2020"],

    /* Optional: silence DOM-related warnings */
    "skipLibCheck": true
  },